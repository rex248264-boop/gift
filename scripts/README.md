# 把你的剧本放在这里

游戏会自动识别 `想见你/` 下所有文件名包含 `S<数字>` 模式的 `.md` 文件作为场次（例如 `03-S01.md`、`04-S02.md`）。

你的剧本目前从 `interactive-script/20260510-103600-想见你/` 拷贝来的副本放在 `想见你/` 子目录下。

修改 `.md` 文件 → 保存 → 浏览器 < 1 秒自动刷新到当前画面。
